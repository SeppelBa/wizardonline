{
  "rules": {
    ".read": false,
    ".write": false,

    "rooms": {
      ".read": true,
      "$roomCode": {
        ".read": true,
        ".write": "$roomCode.matches(/^[A-Z0-9]{4,8}$/)",
        ".validate": "newData.hasChildren(['hostId','phase']) || !newData.exists()",
        "phase": {
          ".validate": "newData.isString() && newData.val().matches(/^(lobby|bidding|playing|choose_trump|round_summary|finished|passing_cards|cloud_adjust)$/)"
        },
        "roundNo":     { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100" },
        "maxRound":    { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100" },
        "createdAt":   { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "updatedAt":   { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "listedAt":    { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "hostId":      { ".validate": "newData.isString() && newData.val().length <= 64" },
        "anniversaryMode": { ".validate": "newData.isBoolean()" },
        "strictBidRule":   { ".validate": "newData.isBoolean()" },
        "fastMode":        { ".validate": "newData.isBoolean()" },
        "paused":          { ".validate": "newData.isBoolean()" },
        "pausedAt":        { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "pausedBy":        { ".validate": "newData.isString() && newData.val().length <= 64" },
        "specials": {
          "$specialKey": {
            ".validate": "newData.isBoolean() && $specialKey.matches(/^(dragon|pixie|bomb|werewolf|shapeshifter|juggler|cloud)$/)"
          }
        },
        "players": {
          "$playerId": {
            "id":              { ".validate": "newData.isString() && newData.val().length <= 64" },
            "name":            { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30" },
            "score":           { ".validate": "newData.isNumber() && newData.val() >= -1000000 && newData.val() <= 1000000" },
            "isBot":           { ".validate": "newData.isBoolean()" },
            "seat":            { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 16" },
            "connected":       { ".validate": "newData.isBoolean()" },
            "lastSeen":        { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "disconnectedAt":  { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "leaveDeadline":   { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "$other": { ".validate": true }
          }
        },
        "spectators": {
          "$spectatorId": {
            "name":     { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30" },
            "id":       { ".validate": "newData.isString() && newData.val().length <= 64" },
            "joinedAt": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "$other": { ".validate": true }
          }
        },
        "$other": { ".validate": true }
      }
    },

    "global_leaderboard": {
      ".read": true,
      "$playerName": {
        ".write": "$playerName.matches(/^[^.#$\\[\\]]{1,30}$/)",
        ".validate": "newData.hasChildren(['wins','gamesPlayed','maxScore','totalScore'])",
        "wins":        { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000000" },
        "gamesPlayed": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000000" },
        "maxScore":    { ".validate": "newData.isNumber() && newData.val() >= -1000000 && newData.val() <= 1000000" },
        "totalScore":  { ".validate": "newData.isNumber() && newData.val() >= -1000000000 && newData.val() <= 1000000000" },
        "$other": { ".validate": false }
      }
    }
  }
}
